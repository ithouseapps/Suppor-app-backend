{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd backend && pip install -r requirements.txt && python manage.py collectstatic --noinput",
    "startCommand": "cd backend && gunicorn academy.wsgi --bind 0.0.0.0:$PORT --workers 4",
    "watchPatterns": ["backend/**"]
  }
}
